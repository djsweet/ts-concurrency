{
  "name": "ts-concurrency",
  "version": "1.0.0",
  "description": "Concurrency utilities for TypeScript",
  "main": "build/index.js",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && jest --rootDir build/tests --testMatch '**/*.js?(x)'"
  },
  "author": "Dani Sweet",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "typescript": "^5.4.5"
  }
}
